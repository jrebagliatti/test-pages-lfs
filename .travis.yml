language: node_js
node_js:
  - 5.1.0
before_scripts:
  - npm install -g mocha
  - npm install git

env:
    - commit = $TRAVIS_COMMIT_RANGE

after_succes:
  - git remote add public git@github.com:jrebagliatti/test-pages-lfs-public.git
  - git push public master